import { questionRating } from 'code-review-ember/helpers/question-rating';
import { module, test } from 'qunit';

module('Unit | Helper | question rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionRating([42]);
  assert.ok(result);
});
